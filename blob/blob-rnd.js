import {Blob,Buffer} from "node:buffer"
import { log } from "node:console"



// const blob = new Blob(["ruhul amin","sdfs"])
// log(blob.size)
// blob.arrayBuffer().then(data=>log(data))
// blob.slice(0,5).text().then(data=>log(data))
// blob.text().then((data)=>log(data))
// blob.bytes().then((data)=>log(data))
// // blob.stream().then(data=>log(data))


// log("default :",Buffer.poolSize)

// const buffer = Buffer.alloc(10)
// log(buffer)


const buf = Buffer.from('buffer');

